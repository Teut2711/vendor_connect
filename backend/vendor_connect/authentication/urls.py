# your_app/urls.py

from django.urls import path, include
from rest_framework.routers import DefaultRouter


urlpatterns = [
    # path(
    #     "api/otp-verification/send-otp/",
    #     OTPVerificationViewSet.as_view({"post": "send_otp"}),
    #     name="send-otp",
    # ),
    # path(
    #     "api/otp-verification/verify-otp-and-register/",
    #     OTPVerificationViewSet.as_view({"post": "verify_otp_and_register"}),
    #     name="verify-otp-and-register",
    # ),
    # Add other URL patterns as needed
]
